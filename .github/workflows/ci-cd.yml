name: Build & Deploy Python AI Backend to EC2 + CloudFront

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create deployment package
        run: |
          mkdir -p deployment-package
          cp -r src/ requirements.txt app.py wsgi.py gunicorn.conf.py deployment-package/ || true
          if [ -d "tests" ]; then
            cp -r tests/ deployment-package/
          fi

      - name: Upload deployment artifact
        uses: actions/upload-artifact@v4
        with:
          name: python-ai-backend
          path: deployment-package/

  deploy:
    runs-on: self-hosted
    needs: build
    timeout-minutes: 15

    steps:
      - name: Validate required secrets
        run: |
          if [ -z "${{ secrets.APP_SECRETS }}" ]; then
            echo "❌ ERROR: APP_SECRETS secret is required but not set"
            exit 1
          fi

      - name: Ensure Python + venv tools installed
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 python3-pip python3-venv python3-setuptools python3-wheel jq
          python3 --version
          pip3 --version

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: python-ai-backend
          path: /home/ubuntu/actions-runner/_work/ai-backend/ai-backend

      - name: Set up application directory
        run: |
          sudo chown -R ubuntu:ubuntu /home/ubuntu/actions-runner/_work/ai-backend/ai-backend
          sudo mkdir -p /var/log/ai-backend
          sudo chown ubuntu:ubuntu /var/log/ai-backend

      - name: Create and activate virtual environment
        working-directory: /home/ubuntu/actions-runner/_work/ai-backend/ai-backend
        run: |
          python3 -m venv venv
          source venv/bin/activate
          pip install --upgrade pip wheel setuptools
          pip install -r requirements.txt
          pip install gunicorn
          deactivate

      - name: Create .env file from secrets
        working-directory: /home/ubuntu/actions-runner/_work/ai-backend/ai-backend
        run: |
          echo '${{ secrets.APP_SECRETS }}' > /tmp/app_secrets.txt

          # Detect if it's JSON or key=value format
          if jq empty /tmp/app_secrets.txt 2>/dev/null; then
            echo "✅ Detected JSON secrets format"
            jq -r 'to_entries|map("\(.key)=\(.value|tostring)")|.[]' /tmp/app_secrets.txt > .env
          else
            echo "✅ Detected key=value secrets format"
            cat /tmp/app_secrets.txt > .env
          fi

          chmod 600 .env
          echo "✅ .env file created successfully"

      - name: Detect app entrypoint
        id: detect_entrypoint
        working-directory: /home/ubuntu/actions-runner/_work/ai-backend/ai-backend
        run: |
          if [ -f "wsgi.py" ]; then
            echo "entrypoint=wsgi:app" >> $GITHUB_OUTPUT
          else
            echo "entrypoint=app:app" >> $GITHUB_OUTPUT
          fi

      - name: Create systemd service
        run: |
          ENTRYPOINT="${{ steps.detect_entrypoint.outputs.entrypoint }}"
          cat << EOF | sudo tee /etc/systemd/system/ai-backend.service
          [Unit]
          Description=Python AI Backend Service
          After=network.target
          Requires=network.target

          [Service]
          Type=simple
          User=ubuntu
          Group=ubuntu
          WorkingDirectory=/home/ubuntu/actions-runner/_work/ai-backend/ai-backend
          Environment=PYTHONPATH=/home/ubuntu/actions-runner/_work/ai-backend/ai-backend
          EnvironmentFile=/home/ubuntu/actions-runner/_work/ai-backend/ai-backend/.env
          ExecStart=/home/ubuntu/actions-runner/_work/ai-backend/ai-backend/venv/bin/gunicorn \\
            --bind 0.0.0.0:8000 \\
            --workers 4 \\
            --timeout 120 \\
            --access-logfile /var/log/ai-backend/access.log \\
            --error-logfile /var/log/ai-backend/error.log \\
            --capture-output \\
            --log-level debug \\
            \$ENTRYPOINT
          Restart=always
          RestartSec=5
          SyslogIdentifier=ai-backend

          [Install]
          WantedBy=multi-user.target
          EOF

      - name: Reload and enable systemd service
        run: |
          sudo systemctl daemon-reload
          sudo systemctl enable ai-backend.service

      - name: Restart application service
        run: |
          sudo systemctl restart ai-backend.service
          sleep 5
          sudo systemctl status ai-backend.service --no-pager -l || true
          echo "Check logs with: sudo journalctl -u ai-backend -n 50 --no-pager"
          echo "Or check: /var/log/ai-backend/error.log"

  cloudfront:
    runs-on: ubuntu-latest
    needs: deploy
    if: always() && needs.deploy.result == 'success'

    steps:
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION || 'us-east-1' }}

      - name: Validate CloudFront configuration
        run: |
          if [ -z "${{ secrets.CLOUDFRONT_ID }}" ]; then
            echo "⚠️ CLOUDFRONT_ID secret not set, skipping CloudFront invalidation"
            exit 0
          fi

      - name: Invalidate CloudFront cache
        run: |
          echo "Invalidating CloudFront cache for distribution: ${{ secrets.CLOUDFRONT_ID }}"
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.CLOUDFRONT_ID }} \
            --paths "/*"
          echo "✅ CloudFront cache invalidation initiated"

  cleanup:
    runs-on: ubuntu-latest
    needs: [build, deploy, cloudfront]
    if: always()

    steps:
      - name: Cleanup workflow
        run: |
          echo "Workflow completed with status:"
          echo "Build: ${{ needs.build.result }}"
          echo "Deploy: ${{ needs.deploy.result }}"
          echo "CloudFront: ${{ needs.cloudfront.result }}"
